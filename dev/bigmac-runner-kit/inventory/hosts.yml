---
all:
  hosts:
    localhost:
      ansible_connection: local

mac_hosts:
  hosts:
    bigmac26:
      ansible_host: 192.168.2.50
      ansible_user: Adam.Gell
      # SSH key already authorized — uses default ~/.ssh/id_ed25519 on control Mac.
      # For a non-default key, add: ansible_ssh_private_key_file: ~/.ssh/some_key
